# BigGhost AI Notes：项目工作约定

本文件适用于整个项目。后续新增或修改知识页面时，先阅读 [创作与样式规范](docs/AUTHORING.md)，再查看实际页面实现。当前用户的明确要求优先于本文件。

## 开始工作

1. 用 `git status` 确认已有修改，保留用户正在进行的工作。
2. 阅读 `docs/AUTHORING.md`；涉及部署时再阅读 `DEPLOYMENT.md`。
3. 首页参照 `site/index.html`；长篇交互笔记参照 `site/notes/compatibility-layers/index.html`。
4. 参考笔记包含多段样式覆盖，以最终生效样式为准，不能只复制文件开头的旧绿色配色。

## 默认交付

- 新笔记放在 `site/notes/<稳定的英文短名>/index.html`。每篇默认是完整、自包含、可离线阅读的单 HTML，CSS、JavaScript 和必要图形内联。
- 新增笔记的任务同时包括：首页新增对应入口卡片、更新标题/简介/日期/标签、更新两处笔记总数、验证跳转。用户明确只要草稿时按其范围执行。
- 站内链接使用相对路径，并显式写出 `index.html`，兼容 GitHub 项目站子路径和本地双击打开。笔记返回首页用 `../../index.html`。
- 首页卡片沿用 `.note-card` 结构；卡片标题、摘要以及 SVG 的 ID 必须在页面内唯一。新增卡片不能复制旧卡片的 `note-title`、`note-summary`、`slab` 等 ID。
- 保持现有苹果风视觉：浅灰背景、白色圆角卡片、蓝紫色强调、克制的玻璃和阴影、系统中文字体；同步适配深色模式、手机与减少动态效果。
- 正文面向普通读者：先明确问题与结论，再给框架图、原理、例子和边界。章节和交互数量按主题决定。
- 网页文案只写给读者。不要出现对话式交付说明、提示词、构建过程、“按你的要求”、未经核查的阅读时长或沿用旧文章的资料核查日期。
- 资料优先使用原始论文、官方文档等一手来源。平台支持范围和版本等易变事实需要重新核查；区分事实、示意和推断。

## 修改边界

- 默认保留品牌、首页首屏及已有文章内容。只改任务需要的部分，不顺带重做视觉、改域名、迁移框架或调整部署。
- 当前发布物为纯静态文件，`site/` 是唯一部署目录。不要为了普通新增文章引入运行时后端、数据库、在线字体、CDN 脚本或必须依赖的 npm 构建链。
- 说明、规范和临时创作材料放在 `site/` 外。不要把未完成模板当作文章发布到首页。
- 新笔记如复用已有交互模块，必须同步正文预渲染内容、脚本数据、打印副本和来源引用；删除模块时同步清理相关脚本。
- 防止列表/折叠项末尾空行、网格卡片拉伸空白和手机横向溢出。定位具体元素的间距，不用全站强制清零或隐藏溢出掩盖问题。

## 完成前

- 内容/布局变更：检查首页到笔记及返回链接，320/390px 手机与桌面布局、深浅色、键盘操作、减少动态效果、打印是否遗漏折叠或可切换内容。
- 脚本变更：检查相关脚本语法和实际交互；不为简单可逆修改引入整套测试工具。
- 仅修改说明文档：检查链接、代码示例和 `git diff --check` 即可，不必重跑页面视觉检查。
- Git 提交、推送和部署按当前任务已有授权执行，不虚构已发布结果。交付说明应明确本地完成、是否已推送，以及实际验证范围。

完整规则、首页卡片示例和新增笔记流程见 [docs/AUTHORING.md](docs/AUTHORING.md)。
